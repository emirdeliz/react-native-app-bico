import * as ProfessionalMock from '../professional';
import * as ChatMock from '../chat';

const contact1 = {
    sender: ProfessionalMock.data[0],
    messages: ChatMock.default,
};

const contact2 = {
    sender: ProfessionalMock.data[2],
    messages: ChatMock.default,
};

const contact3 = {
    sender: ProfessionalMock.data[3],
    messages: ChatMock.default,
};

const contact4 = {
    sender: ProfessionalMock.data[4],
    messages: ChatMock.default,
};

const contact5 = {
    sender: ProfessionalMock.data[5],
    messages: ChatMock.default,
};

const contact6 = {
    sender: ProfessionalMock.data[7],
    messages: ChatMock.default,
};

const contact7 = {
    sender: ProfessionalMock.data[6],
    messages: ChatMock.default,
};

const data = [
    contact1,
    contact2,
    contact3,
    contact4,
    contact5,
    contact6,
    contact7,
];

export default data;
